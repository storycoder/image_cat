<%= form_for @photo, :html  => {:multipart => true } do |f| %>

	<%= f.label :pic %>
	<%= f.file_field :pics, multiples: true  %>

	<%= f.label :category %>
	<%= f.text_field :category %>

	<%= f.label :description %>
	<%= f.text_field :description %>

	<%= f.submit %>
<% end %>